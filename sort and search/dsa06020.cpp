#include<bits/stdc++.h>
using namespace std;
int check(int a[],int &n,int &x)
{
    if(binary_search(a,a+n,x) == 1)
}
int main()
{   
    int t,n,m,x;
    cin>>t;
    while(t--)
    {
        cin>>n>>x;
        int a[n];
        for(int i = 0;i<n;i++)
        {
            cin>>a[i];
        }
        cout<<check
    }
    
}